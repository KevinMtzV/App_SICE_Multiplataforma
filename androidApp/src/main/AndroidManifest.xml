<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.example.app_sice_multiplataforma.provider.READ_DATABASE"
        android:label="Permiso para leer Kardex y Carga"
        android:protectionLevel="normal" />

    <permission
        android:name="com.example.app_sice_multiplataforma.provider.WRITE_DATABASE"
        android:label="Permiso para modificar Kardex y Carga"
        android:protectionLevel="normal" />

    <application
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true"
        android:name=".MarsPhotosApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarsPhotos"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MarsPhotos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.provider.SiceContentProvider"
            android:authorities="com.example.app_sice_multiplataforma.provider"
            android:exported="true"
            android:readPermission="com.example.app_sice_multiplataforma.provider.READ_DATABASE"
            android:writePermission="com.example.app_sice_multiplataforma.provider.WRITE_DATABASE" />

    </application>
</manifest>